<!--pages/check_inform/check_inform.wxml-->
<map class="check-map" latitude="{{latitude}}" longitude="{{longitude}}" markers="{{markers}}" circles="{{circles}}"></map>
<view class="tag-container">
  <view class="tag" bind:tap="handleTransport">可打卡区域</view>
</view>
<i-divider color="#e9eaec" lineColor="#e9eaec" height="12"></i-divider>
<i-panel title="{{job_name}}">
  <i-cell-group>
    <i-cell>上班打卡时间：{{begin_check_time}}</i-cell>
    <i-cell>下班打卡时间：{{end_check_time}}</i-cell>
    <i-cell>打卡地点：{{address}}</i-cell>
  </i-cell-group>
</i-panel>
<i-button class="check-button" bind:click="handleClick" type="success">
  <text>选择</text>
</i-button>

<i-action-sheet visible="{{ action_visible }}" actions="{{ actions }}" show-cancel bind:cancel="handleCancel" bind:click="handleClickItem" />
<i-toast id="toast" />