<!--pages/job-detail/job_detail.wxml
<i-tag
      color="yellow"
      checkable="{{true}}"
      checked="{{true}}"
      type="border"
      class="i-tags"
    >
      {{education}}学历以上
    </i-tag>
    <i-tag
      color="yellow"
      checkable="{{true}}"
      checked="{{true}}"
      type="border"
      class="i-tags"
    >
      只招{{need_gender === 1 ? "男" : "女"}}性
    </i-tag>
-->
<i-panel i-class="title-box">
  <i-cell title="{{job_name}}" label="只招{{need_amount}}人">
    <view slot="icon">
      <i-avatar size="large" shape="square" src="/assets/icons/job.png"></i-avatar>
    </view>
    <view slot="footer">
      <view>
        {{salary}}元/月
      </view>
    </view>
  </i-cell>
</i-panel>
<i-button bind:click="handleClickApply" type="success" shape="circle" size="small">立即报名</i-button>
<i-panel i-class="detail-box">
  <i-cell title="职位描述"></i-cell>
  <i-cell>
    <i-tag
      wx:for="{{tags}}"
      wx:key="{{index}}"
      checkable="{{true}}"
      name="{{index}}"
      color="yellow"
      checked="{{true}}"
      type="border"
      class="i-tags"
    >
      {{item.name}}
    </i-tag>
  </i-cell>
  <i-cell label="{{job_detail}}"></i-cell>
</i-panel>
<i-panel>
  <i-cell-group>
    <i-cell>
      上班时间：{{begin_date}} ~ {{end_date}}
    </i-cell>
    <i-cell>
      报名时间：{{begin_apply_date}} ~ {{end_apply_date}}
    </i-cell>
  </i-cell-group>
</i-panel>

<i-modal visible="{{ visible }}" bind:ok="handleSendApply" bind:cancel="handleClose">
    <view>您确定要应聘这个岗位吗</view>
</i-modal>